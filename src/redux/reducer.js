import {} from "./actions";

const initialState = {
    fState: [],
    sState: [],
    tState: [],
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {

        default: return state;
    }
}